## 2024-05-22 - Memory vs Reality in Optimization
**Learning:** The project memory stated that `initNavbar` was already optimized with `requestAnimationFrame`, but the actual code showed a raw scroll listener. This highlights the importance of always verifying the codebase state rather than relying solely on documentation or memory.
**Action:** Always inspect the target code before skipping an optimization based on assumptions/memory.
